To make the code above clean, you need to update two functions to pass the correct texture:

`player.h` (Update Draw Function)
```c++
// player.h
// Change Draw to take the texture it should draw
void Draw(Rectangle sourceRect, Texture2D textureToDraw);
```

`player.cpp` (Update Draw Implementation)
```c++
// player.cpp
// Implement the new Draw function
void Player::Draw(Rectangle sourceRect, Texture2D textureToDraw) {
    DrawTextureRec(textureToDraw, sourceRect, playerPosition, playerColor);
}
```

Implement these structural logic changes. When you can switch between your soldier's idle and walk animations based on input.