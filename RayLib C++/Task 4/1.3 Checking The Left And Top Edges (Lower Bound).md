Since the left/top corner is (0, 0), the position cannot be negative

```
// Check Left Boundary (X-axis)
if (playerPosition.x < 0) {
    playerPosition.x = 0; // Set it back to the boundary
}
// Check Top Boundary (Y-axis)
if (playerPosition.y < 0) {
    playerPosition.y = 0;
}
```
