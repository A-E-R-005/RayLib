	BeginDrawing(void)
Marks the start of the drawing phase. All drawing commands must be called after this function

	ClearBackground(Color colour)
Fills the entire window with a specified colour. This is necessary to erase the previous frame's content

	EndDrawing(void)
Marks the end of the drawing phase. It handles buffer swapping (displaying the new frame) and polls for input events 